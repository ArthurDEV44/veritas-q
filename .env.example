# =============================================================================
# Veritas Q - Environment Variables
# =============================================================================
# Copy this file to .env and customize as needed.
# All variables have sensible defaults for local development.
# =============================================================================

# -----------------------------------------------------------------------------
# QRNG Configuration
# -----------------------------------------------------------------------------

# ANU QRNG API URL (free public API, used by default for development)
# ANU_QRNG_URL=https://qrng.anu.edu.au/API/jsonI.php?length=1&type=hex16&size=32

# ID Quantique API key (required for production-grade quantum entropy)
# QRNG_API_KEY=

# ID Quantique API URL (production QRNG endpoint)
# QRNG_API_URL=

# -----------------------------------------------------------------------------
# Solana Blockchain Anchor
# -----------------------------------------------------------------------------

# Solana RPC endpoint (default: devnet)
# SOLANA_RPC_URL=https://api.devnet.solana.com

# Path to Solana keypair for anchor transactions
# Required only for `veritas anchor` command
# SOLANA_KEYPAIR_PATH=/path/to/keypair.json

# -----------------------------------------------------------------------------
# Server Configuration (veritas-server)
# -----------------------------------------------------------------------------

# Server port (default: 3000)
# PORT=3000

# Server host (default: 127.0.0.1, use 0.0.0.0 for public access)
# HOST=127.0.0.1

# CORS allowed origins, comma-separated (default: allow all)
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Request body limit in MB (default: 50)
# BODY_LIMIT_MB=50

# Maximum file size per upload in MB (default: 25)
# MAX_FILE_SIZE_MB=25

# Request timeout in seconds (default: 30)
# REQUEST_TIMEOUT_SECS=30

# -----------------------------------------------------------------------------
# WebAuthn Configuration
# -----------------------------------------------------------------------------

# Relying Party ID - must match the domain of the client app
# For production: use your domain (e.g., "veritas-q.com")
# For development: leave as "localhost"
# WEBAUTHN_RP_ID=veritas-q.com

# Primary Relying Party origin URL
# Must match the URL where users access the app
# WEBAUTHN_RP_ORIGIN=https://veritas-q.com

# Additional allowed origins (comma-separated)
# Use this for mobile apps, multiple domains, or staging environments
# WEBAUTHN_RP_ORIGINS=https://app.veritas-q.com,https://staging.veritas-q.com

# Display name for the Relying Party (shown to users)
# WEBAUTHN_RP_NAME=Veritas Q

# Skip port validation (default: false, auto-enabled for localhost)
# Useful when running frontend on different ports during development
# WEBAUTHN_ALLOW_ANY_PORT=false

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------

# Enable rate limiting (default: true in production, false in tests)
# RATE_LIMIT_ENABLED=true

# Requests per second limit (default: 10)
# RATE_LIMIT_PER_SEC=10

# Burst size for rate limiting (default: 20)
# RATE_LIMIT_BURST=20

# -----------------------------------------------------------------------------
# Logging / Tracing
# -----------------------------------------------------------------------------

# Rust log level filter
# RUST_LOG=info
# RUST_LOG=veritas_core=debug,veritas_server=info

# -----------------------------------------------------------------------------
# C2PA (Content Credentials) Configuration
# -----------------------------------------------------------------------------

# Path to PEM-encoded signing key and certificate
# Generate test certs: ./scripts/generate-test-cert.sh
# C2PA_SIGNING_KEY=./keys/c2pa-test.key
# C2PA_SIGNING_CERT=./keys/c2pa-test.crt
#
# For cloud deployments (Render), use Secret Files:
# C2PA_SIGNING_KEY=/etc/secrets/c2pa-signing.key
# C2PA_SIGNING_CERT=/etc/secrets/c2pa-signing.crt
